"use client";
import {useEffect,useState} from "react";
export default function StoresClient(){const[items,setItems]=useState<any[]>([]);const[form,setForm]=useState({name:"",code:"",description:""});async function load(){const r=await fetch("/api/stores");setItems((await r.json()).items??[])}useEffect(()=>{load()},[]);async function create(e:React.FormEvent){e.preventDefault();await fetch("/api/stores",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});setForm({name:"",code:"",description:""});load()}return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Tiendas</h1><p className="text-slate-500">Solo SUPER_ADMIN puede crear y desactivar tenants.</p></div><form onSubmit={create} className="card grid gap-3 p-5 md:grid-cols-3"><input required placeholder="Nombre" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="rounded-lg border p-2"/><input required placeholder="Código único" value={form.code} onChange={e=>setForm({...form,code:e.target.value})} className="rounded-lg border p-2"/><input placeholder="Descripción" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="rounded-lg border p-2"/><button className="rounded-lg bg-slate-900 px-4 py-2 font-semibold text-white md:col-span-3">Crear tienda</button></form><div className="card overflow-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Nombre</th><th>Código</th><th>Estado</th><th>Creada</th></tr></thead><tbody>{items.map(s=><tr key={s.id} className="border-t"><td className="p-4 font-medium">{s.name}</td><td>{s.code}</td><td>{s.active?"Activa":"Inactiva"}</td><td>{new Date(s.createdAt).toLocaleDateString("es-HN")}</td></tr>)}</tbody></table></div></div>}
