"use client";
import {useState} from "react";
export default function Security(){const[f,setF]=useState({currentPassword:"",newPassword:""});const[msg,setMsg]=useState("");async function save(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/profile/security",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(f)});const d=await r.json();setMsg(r.ok?"Contraseña actualizada. Las sesiones anteriores quedan invalidadas.":d.error??"Error");if(r.ok)setF({currentPassword:"",newPassword:""})}return <div className="max-w-xl space-y-6"><div><h1 className="text-3xl font-bold">Seguridad</h1><p className="text-slate-500">Cambie su contraseña y cierre las sesiones anteriores.</p></div><form onSubmit={save} className="card space-y-4 p-5"><input required type="password" placeholder="Contraseña actual" value={f.currentPassword} onChange={e=>setF({...f,currentPassword:e.target.value})} className="w-full rounded-lg border p-3"/><input required minLength={12} type="password" placeholder="Nueva contraseña (12+ caracteres)" value={f.newPassword} onChange={e=>setF({...f,newPassword:e.target.value})} className="w-full rounded-lg border p-3"/><button className="rounded-lg bg-slate-900 px-4 py-3 font-semibold text-white">Cambiar contraseña</button>{msg&&<p className="text-sm text-slate-600">{msg}</p>}</form></div>}
