import {NextRequest,NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/db/prisma";import {hashPassword} from "@/lib/security/password";import {apiAuth,errorResponse} from "@/lib/services/api";import {passwordSchema} from "@/lib/validation/schemas";import {audit} from "@/lib/audit/logger";
const schema=z.object({password:passwordSchema});
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await apiAuth("users.update",request);if("response" in a)return a.response;const {id}=await params;try{const target=await prisma.user.findFirst({where:{id,storeId:a.user.storeId,deletedAt:null}});if(!target)return NextResponse.json({error:"No encontrado"},{status:404});const {password}=schema.parse(await request.json());await prisma.user.update({where:{id},data:{passwordHash:await hashPassword(password),sessionVersion:{increment:1}}});await audit({action:"PASSWORD_CHANGED_BY_ADMIN",entity:"User",entityId:id,storeId:a.user.storeId,userId:a.user.id});return NextResponse.json({ok:true});}catch(e){return errorResponse(e)}}
