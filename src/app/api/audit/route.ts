import {NextRequest,NextResponse} from "next/server";import {prisma} from "@/lib/db/prisma";import {apiAuth,errorResponse} from "@/lib/services/api";
export async function GET(request:NextRequest){const a=await apiAuth("audit.view",request);if("response" in a)return a.response;try{const superGlobal=a.user.roles.some(r=>r.role.name==="SUPER_ADMIN"&&r.role.scope==="GLOBAL");const sp=request.nextUrl.searchParams;const page=Math.max(1,Number(sp.get("page")??1)||1),pageSize=Math.min(100,Math.max(1,Number(sp.get("pageSize")??50)||50));const where=superGlobal?{}:{storeId:a.user.storeId};const [items,total]=await Promise.all([prisma.auditLog.findMany({where,orderBy:{createdAt:"desc"},skip:(page-1)*pageSize,take:pageSize,select:{id:true,storeId:true,userId:true,action:true,entity:true,entityId:true,metadata:true,ipAddress:true,userAgent:true,createdAt:true}}),prisma.auditLog.count({where})]);return NextResponse.json({items,page,pageSize,total});}catch(e){return errorResponse(e)}}
