"use client";
import {useState} from "react";import Link from "next/link";
export default function Forgot(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const d=await r.json();setMsg(d.message)}return <main className="flex min-h-screen items-center justify-center bg-slate-950 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8"><h1 className="text-2xl font-bold">Recuperar contraseña</h1><p className="mt-2 text-sm text-slate-500">Si la cuenta existe, enviaremos instrucciones.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-6 w-full rounded-lg border p-3" placeholder="Correo"/><button className="mt-4 w-full rounded-lg bg-slate-900 px-4 py-3 font-semibold text-white">Enviar instrucciones</button>{msg&&<p className="mt-4 text-sm text-slate-600">{msg}</p>}<Link href="/login" className="mt-5 block text-center text-sm">Volver al login</Link></form></main>}
