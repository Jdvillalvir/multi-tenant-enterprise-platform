"use client";
import {useSearchParams} from "next/navigation";import {useState} from "react";import Link from "next/link";
export default function Reset(){const sp=useSearchParams();const token=sp.get("token")??"";const[p,setP]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password:p})});const d=await r.json();setMsg(r.ok?"Contraseña restablecida.":d.error??"No se pudo completar")}return <main className="flex min-h-screen items-center justify-center bg-slate-950 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8"><h1 className="text-2xl font-bold">Nueva contraseña</h1><input required minLength={12} type="password" value={p} onChange={e=>setP(e.target.value)} className="mt-6 w-full rounded-lg border p-3" placeholder="Nueva contraseña"/><button disabled={!token} className="mt-4 w-full rounded-lg bg-slate-900 px-4 py-3 font-semibold text-white disabled:opacity-50">Restablecer</button>{msg&&<p className="mt-4 text-sm text-slate-600">{msg}</p>}<Link href="/login" className="mt-5 block text-center text-sm">Volver al login</Link></form></main>}
